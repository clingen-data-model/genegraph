[Unit]
Description=ClinGen Search
After=network.target

[Service]
Type=simple
User=app
Group=app
WorkingDirectory=/app
ExecStart=/usr/bin/java -jar /app/lib/app.jar
ExecStop=/bin/kill -TERM $MAINPID

[Install]
WantedBy=multi-user.target
apiVersion: v1
kind: Service
metadata:
  labels:
    app: clingen-ds
  name: clingen-ds
spec:
  type: LoadBalancer
  ports:
    - port: 80
      targetPort: 8888
      protocol: TCP
  selector:
    app: clingen-ds
  
